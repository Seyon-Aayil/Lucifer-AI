{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability set for Lucifer's main + overlay windows on every platform.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
