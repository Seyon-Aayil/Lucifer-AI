{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Lucifer main window. Grants the IPC commands exposed by lucifer-desktop-bindings plus Tauri core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
